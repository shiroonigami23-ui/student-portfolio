name: Deploy (Universal Fix) 🚀

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

# Permissions for GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4  # UPDATED to v4

      # --- THE UNIVERSAL INJECTOR ---
      # Finds FIREBASE_KEY_PLACEHOLDER in any js/html/css/json file and replaces it
      - name: Inject API Key Everywhere
        run: |
          find . -type f \( -name "*.js" -o -name "*.html" -o -name "*.css" -o -name "*.json" \) -exec sed -i "s/FIREBASE_KEY_PLACEHOLDER/${{ secrets.FIREBASE_API_KEY }}/g" {} +

      - name: Setup Pages
        uses: actions/configure-pages@v5  # UPDATED to v5

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3  # UPDATED to v3 (Latest version)
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4  # UPDATED to v4
